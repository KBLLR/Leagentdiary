# LeAgentDiary Environment Configuration
# Copy this file to .env.development or .env.production and configure for your environment

# =============================================================================
# HTDI AGENTIC LAB INTEGRATION
# =============================================================================

# Base URL for HTDI Agentic Lab API
# Development: http://localhost:3000/api
# Production: https://api.htdi.example.com/api (update with your actual domain)
VITE_HTDI_API_URL=http://localhost:3000/api

# Polling interval for diary updates (milliseconds)
# Recommended: 30000 (30 seconds) for dev, 60000 (60 seconds) for prod
VITE_POLL_INTERVAL=30000

# =============================================================================
# FUTURE: STAGE SERVICE (PHASE 2)
# =============================================================================

# Enable stage service features (3D agent profiles, scenes)
# Set to 'true' when leagentdiary-stage-service is operational
VITE_ENABLE_STAGE_SERVICE=false

# Stage service base URL (OpenAI API 3.1.0 compatible endpoints)
# Only used when VITE_ENABLE_STAGE_SERVICE=true
VITE_STAGE_SERVICE_URL=http://localhost:3001/v1

# S3 bucket for agent models (if using stage service)
# Example: s3://leagentdiary-agent-models
VITE_S3_BUCKET_URL=

# =============================================================================
# FUTURE: MEMORY SERVICE (PHASE 3)
# =============================================================================

# Enable memory and reflection features
# Set to 'true' when memory service is operational
VITE_ENABLE_MEMORY_SERVICE=false

# Memory service base URL
VITE_MEMORY_SERVICE_URL=http://localhost:3002/v1

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Application environment (development, staging, production)
VITE_APP_ENV=development

# Enable debug logging in browser console
# Set to 'false' in production
VITE_DEBUG=true

# API request timeout (milliseconds)
VITE_API_TIMEOUT=10000

# Maximum retries for failed API requests
VITE_API_MAX_RETRIES=3

# =============================================================================
# OPTIONAL: ANALYTICS & MONITORING
# =============================================================================

# Google Analytics tracking ID (optional)
# VITE_GA_TRACKING_ID=

# Sentry DSN for error tracking (optional)
# VITE_SENTRY_DSN=

# =============================================================================
# OPTIONAL: FEATURE FLAGS
# =============================================================================

# Enable experimental features
VITE_ENABLE_EXPERIMENTAL=false

# Enable WebSocket real-time updates (instead of polling)
# Requires WebSocket support in HTDI backend
VITE_ENABLE_WEBSOCKET=false

# Enable advanced filtering (graph view, heatmap)
# Phase 4 features
VITE_ENABLE_ADVANCED_FILTERING=false

# =============================================================================
# NOTES FOR DEVELOPERS
# =============================================================================
#
# 1. All environment variables must be prefixed with VITE_ to be accessible
#    in the browser via import.meta.env.VITE_*
#
# 2. Never commit .env.development or .env.production files to Git
#    They may contain sensitive information
#
# 3. For local development, copy this file:
#    cp .env.example .env.development
#
# 4. For production builds, ensure all required variables are set in your
#    hosting platform's environment settings (Netlify, Vercel, etc.)
#
# 5. See CLAUDE.md (Task 1.3) for detailed environment configuration guide
#
# =============================================================================
